import React, { Component } from 'react'

interface forProps {
    name: string,
    id: number | string
}

export default class Child extends Component <forProps,{}> {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

